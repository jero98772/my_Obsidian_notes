aws have

aws let script the api calls 
cli, let do api calls using local computer and terminal and help to automatizate it

aws developer kit , sdk let interact with aws , advantage of this 

aws beanstalk, make easy to save enviroments and export it

there is also aws elastic beanstalk

aws cloudformation use json or yalm as programing language for infrastructure

aws cloudformation i can use from multiples regions and multiples regions and will setup the infrastructrue automatically

**Amazon CloudFront** is a Content Delivery Network (CDN)
cloudfront, a service for delivery content in a global way


AWS edge zone is for chaching, make less latency and security

 AWS Outposts is a hybrid cloud service that extends native AWS infrastructure
route 53 dns serivce of aws

Amazon RDS (Relational Database Service)
Amazon DynamoDB  (NoSQL Database Service)

AWS elastic block store

Amazon EBS (Elastic Block Store)

Amazon EFS (Elastic File System)

aws is compatible with 
mysql
postgress
microsoft sql
oracle
amazon realtional database suport the before databases

it have
recovery
redundancy
security copies
automatic patch 

amazon aurora come in 2 forms mysql and postgress , 10 times more cheaper than comercial database, continues backups in s3

amazon dynamo db is  a elastic database

amazon redshift, is  data warehouse services for big data tasks

amazon also have migration service, dms, database migration service, while you do the migration the database still operational

amazon dms It enables the migration of relational databases, non-relational databases, and other types of data stores.

amazon neptune is a graph database of aws, it have inmutability

amazon managed blockchain
amazon quantum lednger database qldb
Amazon ElastiCache

you need to give permisions to the user to create S3 or EC2 instances

For example, if a cashier becomes an inventory specialist, the coffee shop owner removes them from the **"Cashiers"** IAM group and adds them to the **"Inventory Specialists"** IAM group. This ensures that employees have only the permissions required for their current role.

the root user can pay for all the team

**AWS Artifact** is a service that provides on-demand access to security reports.

DDOS, can be slow , and can send info for 1 ip and recive in other one, 
udp flow -> via security groups

aws sheild and aws waf

kms , service for save keys
aws inspector help to improve security

cloud watch can handle alarmas and metrics
cloud watch have a dashboard in real time
aws cloud trail
monitor api and let search in that log

aws trusted advisor 
optimize cost
handle security and performance 

Free aws

always free
12 montsh free
tests

[AWS Pricing Calculator](https://calculator.aws/#/)

to 5gb max in free tiar in s3

aws cost explorer


AWS Cloud Adoption Framework

for data migration AWS Snowcone, AWS Snowball y AWS Snowmobile

six migration strategies :

- **Rehost**
- **Replatform**
- **Refactor / Rearchitect**
- **Repurchase**
- **Retain**
- **Retire**
  
  Aws have this AI services
  
  - Convert speech to text with **Amazon Transcribe**.
    
- Discover patterns in text with **Amazon Comprehend**.
    
- Identify potentially fraudulent online activities with **Amazon Fraud Detector**.
    
- Create voice and text chatbots with **Amazon Lex**.